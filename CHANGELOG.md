# Changelog

## 0.3.2

- 接入五段用户提供的章节结算 MP4；找齐 8 个目标后自动播放约 5 秒的剧情过场。
- 新增过场标题、承上启下字幕和跳过操作；视频结束后再展示下一关／重玩结算入口。

## 0.3.1

- 将五关改为完整的“潮汐星图案”证据链，并在进入每关时增加案件档案与本关调查目标。
- 新增“潮汐钟塔地库：沉没证词”与“制图师密室：星图终章”两张独立 1536×1024 场景背景，固定关卡不再复用前序背景。
- 场景目标 Sprite 改为低透明度命中／反馈层，物品视觉自然融入背景，提升寻物难度。

## 0.3.0

- 增加 `packages/level-engine` 与 Fastify API：Prompt → Intent → 预制场景/物品 → seed → Level JSON → Runtime。
- 增加 mock/OpenAI 双模式、规则校验、失败回退、生成记录、读取关卡与埋点接口。
- 首页加入“一句话生成并开始”，生成成功后直接进入 Phaser。

## 0.2.0

- 新增统一 UI Token、五个展示关卡、找到物品飞向任务栏动画、任务栏完成动效、声音/音乐开关及客户端埋点。
- 补齐加载、生成、错误与结算状态，并保持游戏页任务栏固定可见。

## 0.1.3

- 完成 V0.1 的分层目标资产：三张背景层与 24 个透明 PNG 目标 Sprite，Phaser 以 Sprite 加载、显示、命中和反馈。
- 加入首页选关；Level JSON 通过 Zod 与任务 ID 规则校验，配置非法时显示友好错误页。
- 新增三关资源完整性、目标数量与命中区不重叠的回归测试；修复三处热区重叠。

## 0.1.2

- 新增连贯的三章案件线“失踪制图师与星图”：侦探书桌 → 旧城档案馆 → 雾港船长舱。
- 生成并接入第 2、3 关的 1536×1024 高密度场景，以及各自的 8 个缩略图、热点与剧情化提示。
- 运行时改为按 Level Schema 加载场景；顶部可切换章节，后续可独立追加剧情、目标和结算文案。

## 0.1.1

- 目标栏改为背景物品的局部缩略图，不再显示文字或通用符号图标。
- 点击目标缩略图会定位对应物品，并显示不直接透露名称的提示语。
- 收缩相邻的怀表和钥匙热区，移除大矩形找到反馈。

## 0.1.0

- 建立 pnpm workspace、React/Vite 和 Phaser 首关运行时。
- 加入“雨夜侦探书桌”背景、8 个目标热点、提示、误点、任务栏与通关结算。
- 加入共享 Level Zod Schema 和基础契约测试。
